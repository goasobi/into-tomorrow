alias curltime "curl -w @$HOME/.curlformat.txt -o /dev/null -s "

alias gd lazydocker
alias gg lazygit

alias python python3
alias ranger "ranger --choosedir=$HOME/.rangerdir; cd (cat $HOME/.rangerdir)"

alias vi nvim
alias vim "nvim -N -u ~/.vimrc"
